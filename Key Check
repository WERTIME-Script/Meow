local ScreenGui    = Instance.new("ScreenGui")
local MainFrame     = Instance.new("Frame")
local UICorner      = Instance.new("UICorner")
local UIStroke      = Instance.new("UIStroke")
local Title         = Instance.new("TextLabel")
local SubTitle      = Instance.new("TextLabel")
local KeyInput      = Instance.new("TextBox")
local CheckBtn      = Instance.new("TextButton")
local CloseBtn      = Instance.new("TextButton")
local MiniBtn       = Instance.new("TextButton")
local RunService    = game:GetService("RunService")
local TweenService  = game:GetService("TweenService")
local UIS           = game:GetService("UserInputService")
local Debris        = game:GetService("Debris")

-- =========================================================
-- [ 🟢 CHỈNH SỬA THÔNG TIN TẠI ĐÂY ]
-- =========================================================
local MY_NAME     = "ModX.Check-Key" 
local MY_SIGN     = "•By.ModX"        

local KEYS_LIST = {
    ("lol"),
    ("Key_02"),
    ("Key_03"),
    ("Key_04"),
    ("Key_05"),
    ("Key_06"),
    ("Key_07"),
    ("Key_08"),
    ("Key_09"),
    ("Key_10")
}
-- =========================================================

local isLock = false 
local COLOR_BLURPLE = Color3.fromRGB(88, 101, 242)
local COLOR_RED     = Color3.fromRGB(237, 66, 69)
local COLOR_GREEN   = Color3.fromRGB(87, 242, 135)

-- [ SETUP UI ] --
ScreenGui.Parent = game.CoreGui
MainFrame.Name = "ModX_Final_Supreme"
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0, 320, 0, 200)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -100)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
MainFrame.ClipsDescendants = true

UICorner.CornerRadius = UDim.new(0, 12); UICorner.Parent = MainFrame
UIStroke.Thickness = 2.5; UIStroke.Color = COLOR_BLURPLE; UIStroke.Parent = MainFrame

-- [ TIÊU ĐỀ & CHỮ KÝ ] --
Title.Size = UDim2.new(0, 200, 0, 30); Title.Position = UDim2.new(0, 15, 0, 8)
Title.RichText = true; Title.Text = "<font face='RobotoMono' size='14'><b>"..MY_NAME.."</b></font>"
Title.TextColor3 = Color3.fromRGB(220, 221, 222); Title.TextXAlignment = Enum.TextXAlignment.Left; Title.BackgroundTransparency = 1; Title.Parent = MainFrame

SubTitle.Size = UDim2.new(0, 200, 0, 20); SubTitle.Position = UDim2.new(0, 15, 0, 26)
SubTitle.Text = MY_SIGN; SubTitle.Font = Enum.Font.RobotoMono; SubTitle.TextSize = 10
SubTitle.TextColor3 = Color3.fromRGB(114, 118, 125); SubTitle.TextTransparency = 0.4; SubTitle.TextXAlignment = Enum.TextXAlignment.Left; SubTitle.BackgroundTransparency = 1; SubTitle.Parent = MainFrame

-- [ NÚT ĐÓNG & GẬP - ĐÃ FIX DẤU X ] --
CloseBtn.Size = UDim2.new(0, 30, 0, 30); CloseBtn.Position = UDim2.new(1, -35, 0, 5)
CloseBtn.Text = "X"; CloseBtn.TextColor3 = COLOR_RED; CloseBtn.TextSize = 18; CloseBtn.Font = Enum.Font.GothamBold; CloseBtn.BackgroundTransparency = 1; CloseBtn.Parent = MainFrame

MiniBtn.Size = UDim2.new(0, 30, 0, 30); MiniBtn.Position = UDim2.new(1, -65, 0, 5)
MiniBtn.Text = "-"; MiniBtn.TextColor3 = Color3.new(1, 1, 1); MiniBtn.TextSize = 25; MiniBtn.Font = Enum.Font.GothamBold; MiniBtn.BackgroundTransparency = 1; MiniBtn.Parent = MainFrame

-- [ Ô NHẬP KEY ] --
KeyInput.Size = UDim2.new(0.85, 0, 0, 40); KeyInput.Position = UDim2.new(0.075, 0, 0.38, 0)
KeyInput.BackgroundColor3 = Color3.fromRGB(30, 31, 34); KeyInput.TextColor3 = Color3.fromRGB(220, 221, 222)
KeyInput.Font = Enum.Font.RobotoMono; KeyInput.TextSize = 12; KeyInput.Text = ""; KeyInput.ClearTextOnFocus = true; KeyInput.Parent = MainFrame
Instance.new("UICorner", KeyInput).CornerRadius = UDim.new(0, 6)

-- [ HÀM BIẾN MẤT TỪ TỪ (FADE OUT) ] --
local function FadeOutAndDestroy()
    isLock = true
    local info = TweenInfo.new(0.6, Enum.EasingStyle.Quart, Enum.EasingDirection.Out)
    TweenService:Create(MainFrame, info, {BackgroundTransparency = 1}):Play()
    TweenService:Create(UIStroke, info, {Transparency = 1}):Play()
    for _, v in pairs(MainFrame:GetDescendants()) do
        if v:IsA("TextLabel") or v:IsA("TextBox") or v:IsA("TextButton") then
            TweenService:Create(v, info, {TextTransparency = 1, BackgroundTransparency = 1}):Play()
        end
    end
    task.wait(0.6)
    ScreenGui:Destroy()
end

-- [ HIỆU ỨNG IMPORTING... ] --
task.spawn(function()
    while task.wait(0.5) do
        if not KeyInput:IsFocused() and KeyInput.Text == "" then
            KeyInput.PlaceholderText = "Importing."
            task.wait(0.5); KeyInput.PlaceholderText = "Importing.."
            task.wait(0.5); KeyInput.PlaceholderText = "Importing..."
        else KeyInput.PlaceholderText = "" end
    end
end)

-- [ FAIL FEEDBACK (RUNG + ĐỎ) ] --
local function Denied()
    local origin = MainFrame.Position
    local start = tick()
    UIStroke.Color = COLOR_RED; CheckBtn.BackgroundColor3 = COLOR_RED
    CheckBtn.Text = "<font face='RobotoMono' size='13'><b>! ERROR_KEY</b></font>"
    local conn
    conn = RunService.RenderStepped:Connect(function()
        if tick() - start < 0.4 then
            MainFrame.Position = origin + UDim2.new(0, math.random(-7, 7), 0, math.random(-7, 7))
        else
            MainFrame.Position = origin; UIStroke.Color = COLOR_BLURPLE
            CheckBtn.BackgroundColor3 = COLOR_BLURPLE
            CheckBtn.Text = "<font face='RobotoMono' size='13'><b>> RUN_AUTH</b></font>"; isLock = false; conn:Disconnect()
        end
    end)
end

-- [ NÚT VERIFY (XANH LÁ) ] --
CheckBtn.Size = UDim2.new(0.85, 0, 0, 42); CheckBtn.Position = UDim2.new(0.075, 0, 0.68, 0)
CheckBtn.RichText = true; CheckBtn.Text = "<font face='RobotoMono' size='13'><b>> RUN_AUTH</b></font>"
CheckBtn.BackgroundColor3 = COLOR_BLURPLE; CheckBtn.TextColor3 = Color3.new(1, 1, 1); CheckBtn.Parent = MainFrame
Instance.new("UICorner", CheckBtn).CornerRadius = UDim.new(0, 6)

CheckBtn.MouseButton1Click:Connect(function()
    if isLock then return end
    isLock = true
    local match = false
    for _, k in pairs(KEYS_LIST) do if KeyInput.Text == k then match = true break end end
    if match then
        UIStroke.Color = COLOR_GREEN; CheckBtn.BackgroundColor3 = COLOR_GREEN
        CheckBtn.Text = "<font face='RobotoMono' size='13'><b># SUCCESS</b></font>"
        FadeOutAndDestroy()
    else Denied() end
end)

CloseBtn.MouseButton1Click:Connect(FadeOutAndDestroy)

local min = false
MiniBtn.MouseButton1Click:Connect(function()
    min = not min
    TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Quart), {Size = min and UDim2.new(0, 320, 0, 50) or UDim2.new(0, 320, 0, 200)}):Play()
    KeyInput.Visible, CheckBtn.Visible, SubTitle.Visible = not min, not min, not min
end)

-- [ KÉO THẢ CHỐNG LOẠN (KHÓA ID NGÓN TAY) ] --
local dragToggle, dragStart, startPos, dragInput
MainFrame.InputBegan:Connect(function(input)
    if not dragToggle and (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
        dragToggle = true; dragInput = input; dragStart = input.Position; startPos = MainFrame.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if dragToggle and input == dragInput then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(input)
    if input == dragInput then dragToggle = false; dragInput = nil end
end)
